# -------------------------------------------------------------
# AI 주식 자동매매 봇 (AI Quant) 필수 패키지 목록
# -------------------------------------------------------------

# 구글 Gemini 연동 (생성형 AI)
google-genai==0.3.0

# 텔레그램 알림 및 웹 HTTP 요청 통신
requests==2.31.0

# 데이터 처리 및 테이블 분석 (가장 핵심적인 수학 라이브러리 연동)
pandas>=2.2.0
numpy>=1.26.4

# 환경변수 파일(.env, .ini) 관리
python-dotenv==1.0.1
configparser==6.0.0

# API 서버 등에서 받아온 데이터 규격(Type) 검증
pydantic>=2.6.1

# 거시 경제 지표 조회 (VIX 공포지수, 나스닥 등 금융데이터 수집)
finance-datareader==0.9.60

# 한국 거래소 (KRX) 국내 주식시장 시세 및 종목 정보 크롤링
pykrx==1.0.45

# 한국투자증권 OPEN API 편의 래퍼 (선택적 보조 도구)
mojito2
